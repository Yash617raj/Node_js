/*
    An HTTP header is piece of information send to the client(browser). It contains key details such as
    (So basically the packet we send has extra information on them that give the information about the packet)
    this is a meta-data(data about a data)
    what type of content is being sent(json,image etc);
    the language (english or spanish)
    instruction on how to handle the data
    from 122.10.10.2 to 123.24.42.5 it could also be like this
    i will look into it after coming back
*/